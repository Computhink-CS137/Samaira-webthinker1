let squareX;
let squareY;
function setup() {
    new Canvas(600, 400);
    background(220);
}

function draw() {
    // movemet; change square y & x
    squareX += 5;
    fill(30, 255, 0);
    rect(, height/2, 100, 100)
}